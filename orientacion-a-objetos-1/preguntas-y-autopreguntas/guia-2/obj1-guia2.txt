# Objetos 1 - preguntas y auto preguntas de guia 2

�Qu� es el polimorfismo?
La palabra significa "muchas formas", es
cuando una clase al heredar su comportamiento
a otra, es sobre-escrito

�Cuando un metodo esta sobrecargado?
Cuando resuelve un mismo caso de uso y en su
firma se diferencia en los parametros que recibe;
es un tipo de polimorfismo

�Que es una relaci�n de Agregaci�n?
Es, cuando una clase incluye a otra,
la destruccion de la clase principal
no implica la destruccion de la clase que contiene

�Que es una relacion de Composici�n?
Es, cuando una clase incluye a otra,
la destruccion de la clase principal
implica la destruccion de la clase que contiene

�Por que un metodo solo debe cambiar
aquellos objetos que se hayan creado en su ambito?

Tanto la clase Punto como la clase Circulo tienen un m�todo llamado calcularDistancia �Esto es un
ejemplo de Polimorfismo? �Es un ejemplo de Sobrecarga?
(resolver)

�En que se diferencia la agregacion de la composicion?**consultar**

�Que puede suceder si dos objetos encapsulan el mismo objeto?**consultar**

�En un sistema en producci�n es conveniente modificar la �firma� de un m�todo (su nombre, su tipo
de retorno y sus par�metros)?

¿Que es un array unidimensional?
Es una estructura que almacena datos del
mismo tipo

¿Como se define un array unidimensional sin inicializar?
tipoDeDato[] nombre = new tipoDeDato[longitudArray];

¿Como se define un array unidimensional inicializando?
tipoDeDato[] nombre = {datos};

¿En que posicion arranca un array?
La primer posicion de un array siempre arranca en 0

¿Cuales son los valores por defecto de un array sin inicializar?
- Numericos -> 0
- Cadenas y letras -> (vacio)
- Booleanos -> false

¿Como se define un array bidimensional sin inicializar?
tipoDeDato[] nombre = new tipoDeDato[longitudFilas][longitudColumnas];

¿Como se define un array bidimensional inicializando?
tipoDeDato[] nombre = {{datos} {datos} {datos}};